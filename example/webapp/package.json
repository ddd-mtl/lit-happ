{
  "name": "example",
  "version": "0.0.1",
  "description": "Example app for testing",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "scripts": {

    "lint": "eslint --ext .ts .",
    "tsc": "rm -rf dist && tsc",

    "build:ui": "rm -rf dist && vite build",

    "start": "UI_PORT=$(port) HC_APP_PORT=$(port) HC_ADMIN_PORT=$(port) HAPP_BUILD_MODE='Debug' concurrently \"npm run new-sandbox:mem\" \"sleep 5 && npm run start:ui\"",
    "start:clone": "HAPP_BUILD_MODE='Debug' HC_APP_PORT=$(port) HC_ADMIN_PORT=$(port) concurrently \"npm run new-sandbox:mem:clone\" \"sleep 5 && npm run start:ui\"",
    "new-sandbox:mem": "RUST_LOG=trace WASM_LOG=trace echo \"pass\" | ../../bin/hc s --piped -f=$HC_ADMIN_PORT generate ../../artifacts/playground.happ --run=$HC_APP_PORT -a playground network mem",
    "new-sandbox:mem:clone": "RUST_LOG=trace WASM_LOG=trace echo \"pass\" | ../../bin/hc s --piped -f=$HC_ADMIN_PORT generate ../../artifacts/playground-clone.happ --run=$HC_APP_PORT -a playground-clone network mem",

    "start:ui": "vite dev --port $UI_PORT --clearScreen false",

    "launch": "RUST_LOG=warn echo \"pass\" | ../../bin/hc launch --piped ../../artifacts/playground.happ -w --ui-path ./dist network mem",
    "launch:clone": "RUST_LOG=warn echo \"pass\" | ../../bin/hc launch --piped ../../artifacts/playground-clone.happ -w --ui-path ./dist network mem",

    "clean:hc": "hc s clean"
  },
  "devDependencies": {
    "vite": "4.5.14",
    "vite-plugin-checker": "=0.5.3",
    "vite-plugin-dts": "4.5.3",
    "vite-plugin-top-level-await": "1.5.0",
    "bestzip": "^2.2.0",
    "concurrently": "^5.3.0",
    "deepmerge": "^4.2.2",
    "exits": "^2.0.1",
    "new-port-cli": "^1.0.0",
    "path-exists-cli": "^2.0.0",
    "run-singleton-cli": "^0.0.7",
    "tslib": "^2.4.0",
    "typescript": "~5.2.0"
  },
  "dependencies": {
    "@ddd-qc/lit-happ": "file:../",
    "@holochain-open-dev/core-types": "0.7.0",
    "@ddd-qc/profiles-dvm": "file:../",
    "@lit/context": "1.0.1",
    "lit": "3.0.2"
  }
}
